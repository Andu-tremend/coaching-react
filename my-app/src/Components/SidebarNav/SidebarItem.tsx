import React from "react";

export default function SidebarItem(props: any) {  // to change props interface
    

    return (
        <div>
            {props.name}
            <div>LOREM LOREM LOREM</div>
        </div>
    )
}